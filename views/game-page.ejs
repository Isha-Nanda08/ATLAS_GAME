<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style/game-page.css">
    <title>Game Page</title>
</head>
<body>
    <div class="main-content">
        <div class="container1"></div>
        <h1>ATLAS-MANIA</h1>
        <div class="player-info">
            <ul>
                <% for (let i=0; i<allPlayers.length; i++) { %>
                    <% let currPlayer = allPlayers[i]; %>
                    <li>
                        <p class="nameP">🎖️<%= currPlayer.name %>
                            <div class="status">
                                <!-- ⏳ this is timer -->
                                <% if (!livePlayers.some(player => currPlayer.name != "Atlas-AI" && player.ip == currPlayer.ip)) { %>
                                    🚩
                                <% } %>
                                <% if (!hasTurn) { %> ▶️ <% } %>
                            </div>
                        </p>
                        <p>❤️<%= allPlayers[i].lives %> lives</p>  
                        <button class="on ">💡Hints(<%= currPlayer.hints %>)</button>
                    </li>
                <% } %>
            <ul>
                
        </div>
        
        
        <div class="fun-fact">
            <p class="room-name"><%= name %></p>
            <p><span class="prev-player">previous-player:whesks</span> <span class="prev-word">word spoken: <span>India</span></span></p>
            <p><span class="current-player">current-player: <% livePlayers[currPlayer].name %> </span> <span class="current-alpha">current alphabet :</p>
            <p class="alpha"><%= currChar %></p>
            <div class="box-in">
            
                <input placeholder="enter region " class="input-count" name="text" type="text">
                <p><button class="submit">submit
                    <span class="first"></span>
                    <span class="second"></span>
                    <span class="third"></span>
                    <span class="fourth"></span>
                    </button></p>
            </div>

            <div class="leave-btn"><button class="leave">leave-room</button></div>
        </div>

        <div class="map-svg">
            <%- include("svgs/world 1.ejs") %>
        </div>
        <div class="current-loc"></div>
    </div>
</body>
</html>